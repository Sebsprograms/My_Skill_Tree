import 'package:flutter/material.dart';

class Skill {
  String id;
  String name;
  String description;
  Color color;

  Skill({
    required this.id,
    required this.name,
    required this.description,
    required this.color,
  });
}
